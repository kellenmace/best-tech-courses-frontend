import React from 'react';
import Spinner from 'react-spinkit';

export default () => (
  <div style={{ textAlign: 'center' }}>
    <Spinner name="pacman" fadeIn="quarter" style={{ display: 'inline-block' }} />
  </div>
);
